# SAINT — Python 3.12+ (developed and tested on Python 3.14, Windows 11)
#
# PyTorch: Kokoro TTS runs on PyTorch. For GPU speech output install the CUDA
# build FIRST (RTX 50-series / Blackwell needs CUDA 12.8+ wheels), e.g.
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch torchaudio
# then install the rest with:  pip install -r requirements.txt

# UI / core
PySide6>=6.6.0
requests>=2.31.0
psutil>=5.9.0
numpy>=1.24.0

# Audio, wake word, speech recognition
sounddevice>=0.4.6
soundfile>=0.12.1
onnxruntime>=1.16.0          # runs the hey_saint.onnx wake-word model (CPU)
faster-whisper>=1.0.3        # speech-to-text (CTranslate2; bundles its own CUDA runtime)

# Speech output
torch>=2.2.0
kokoro>=0.7.0
misaki[en]>=0.7.0
espeakng_loader>=0.2.4       # espeak-ng for out-of-dictionary words (e.g. names)
phonemizer>=3.2.0

# Desktop control / screen awareness (Windows)
pyautogui>=0.9.54
pywin32>=306
uiautomation>=2.0.18
pillow>=10.0.0

# What's playing in any app (mini player): Windows' media controls
winrt-runtime>=3.0
winrt-Windows.Media.Control>=3.0
winrt-Windows.Foundation>=3.0
winrt-Windows.Foundation.Collections>=3.0
winrt-Windows.Storage.Streams>=3.0

# Spotify login tokens are stored in the Windows Credential Manager
keyring>=25.0.0

# Tests
pytest>=8.0.0
